# 案例验收与测试基线

## 2026-09-13 边界回归补充

本轮以远程 `52ff7de` 为基线优化，种子数据和三批订单不变。下面先记录新增验收项；
后文保留里程碑 4 的历史记录与里程碑 6 的模型口径修正。

| 输入/操作 | 修复后的预期行为 | 回归文件 |
| --- | --- | --- |
| 严重度 5，明确供应削减 0% | 保持 0%，不猜测为 75% | `tests/test_scenario_edges.py` |
| EVT-04 正常交期 12 + 延长 2，当前已为 16 周 | 自动推演交期不低于 16 周 | `tests/test_scenario_edges.py` |
| 无订单关联某依赖/空多依赖冲击 | 时间线正常、订单表保留完整列、提示未评估 | `tests/test_scenario_edges.py` |
| 既有额外库存 12 周，再追加 8 周 | 按总计 20 周额外库存推演，费用只计本次 8 周 | `tests/test_scenario_edges.py` |
| 全部权重为 0 / 只有事件权重非零 | 回落默认 / 主因子按事件加权贡献选取 | `tests/test_risk_edges.py` |
| resolved 事件加载、刷新并启用假设模式 | 保持解除，不进入冲击和待核实提醒 | `tests/test_repository_validation.py` |
| 缺列、主键重复、外键不存在、非法数值 | 加载时给出文件/列/行提示，不进入计算 | `tests/test_repository_validation.py` |
| 同订单同组件拆分成多行 | 数量相加、订单金额不重复 | `tests/test_repository_validation.py` |
| 图例、节点、订单数量标签布局 | 图例独立在绘图区外，标签不相互重叠 | `tests/test_graph.py` |
| 事件选择/数据模式/CSV 变化 | 清除旧并行结果及旧 AI 解读，提示重新推演 | `tests/test_ui.py` |
| 空巡检/更改来源 | 清除旧信号，旧勾选不套用新批次 | `tests/test_ui.py` |
| 模拟文件替换失败/批内单条抽取异常 | 原文件与输入保持，其他条目仍可处理 | `tests/test_ingest_edges.py` |
| 打包含合成凭据、坏 PPT、越界链接或写入失败 | 拒绝/清理残包，旧包不被覆盖 | `tests/test_package.py` |

运行：`python -m unittest discover -s tests -v`、`python scripts/cases.py`、
`python scripts/demo.py`。测试使用临时数据与请求替身，不读写真实事件库或使用真实 Key。

---

> 初版：2026-09-04；口径定稿：2026-09-07
> 基线提交：`699a519`（里程碑 3）
> 运行环境：Python 3.14.3；`python scripts/demo.py` 冒烟演示通过。
> 本文档为“案例 A–D 预期 vs 实测”的基线记录，用于决定里程碑 4 是否需要调整
> 种子数据、推演参数或补充边界处理逻辑。文中所有企业、订单与事件均为赛题虚构。

## 已定口径（2026-09-07）

按推荐口径推进，**不改动赛题“三批订单”的种子设定**：

- 案例 A/B 的验收点是“断供点预警 + 现有订单不受影响 + 断供后新增订单无缓冲提示 +
  应对方案比较”，不要求产生“受影响订单金额”。
- 案例 C 的验收点是“上游信息缺失被量化并给出授权链/人工尽调的行动建议”。
- 案例 D 的验收点是“低置信度/传闻事件默认不参与评分与推演；只有显式假设分析时才启用”。

对应实现与回归结果见文末“里程碑 4 执行记录”。

## 基线快照（当前引擎输出）

- 依赖图谱：9 节点、11 条边；暴露度排名 DEP-02（71.5，高）> DEP-01（52.5，中）
  > DEP-03（39.5，低）。
- 事件库：EVT-01/02/04 为 active；EVT-03 为 verify + low confidence + rumor。
- 情景推演 v1：按“库存 + 在途订单 + 持续补货”逐周推演，可输出断供周次与订单状态。

---

## 案例 A：日本高精度编码器出口审查收紧

**复现方式**：事件 EVT-01 → `shocks_from_events` 自动转为 DEP-01 冲击参数
（新交期 = 正常 8 周 + 12 周 = 20 周，供应削减 0%）。

**预期**：能推演出断供时间点，并给出受影响订单清单与影响金额，供决策者评估应对方案。

**实测**：

| 项目 | 实测值 |
| --- | --- |
| 转出的推演参数 | 供应削减 0%；新交期 20 周 |
| 首个当周缺口 | 第 18 周（第 17 周末库存恰好为 0） |
| 受影响订单数 | 0 |
| 受影响金额（万元） | 0 |
| 订单状态 | 3 个订单（due 8/12/16 周）均在断供前可交付 |

**结论（已定口径）**：推演区分“周末库存归零”和“当周需求未满足”；现有 3 个订单
交付期都早于第 18 周，因此“受影响订单”为空是符合赛题设定的结果。案例重点定为“首个
当周缺口预警 + 第 18 周后
新增订单无库存支撑提示 + 应对方案比较”，不改动三批订单的种子数据。

---

## 案例 B：红海/关键海峡航运中断 → 芯片到货延迟

**复现方式**：事件 EVT-02 → `shocks_from_events` 自动转为 DEP-02 冲击参数
（在途延误 4 周；新交期 = 当前 16 周 + 4 周 = 20 周）。

**预期**：在途延迟传导至库存消耗与生产排期，展示跨组件订单的连锁影响。

**实测**：

| 项目 | 实测值 |
| --- | --- |
| 转出的推演参数 | 在途延误 4 周；新交期 20 周 |
| 首个当周缺口 | 第 17 周 |
| 受影响订单数 | 0 |
| 受影响金额（万元） | 0 |

**结论（已定口径）**：延迟已进入引擎并被量化（在途延误 4 周、新交期 20 周）；
按推荐口径不调整种子订单，案例演示“首个当周缺口预警 + 第 17 周后无缓冲”，
并用多事件/方案比较补充决策场景。

---

## 案例 C：工业控制芯片上游信息缺失

**预期**：系统明确标注 DEP-02 上游生产与授权关系不明，量化“信息可见性”风险，
不编造上游事实，并给出补全路径（索取授权链 / 人工尽调）。

**实测**：

| 行为 | 当前状态 |
| --- | --- |
| 上游是否已知 | DEP-02 = 否（`upstream_known=0`） |
| 信息可见性风险 | 100 分（满格） |
| 暴露度影响 | DEP-02 综合 71.5，最高风险，主要受可见性/集中度/事件共同推动 |
| 图谱表达 | 依赖图画为红色虚线（上游不明） |
| 明确“高不确定性”提示 | 缺少面向用户的结论级提示 |
| 补全路径/行动建议 | 仅存在于种子数据备注，未在报告与 UI 中结构化输出 |

**结论（已实现）**：风险量化和图谱标注原本已具备；里程碑 4 新增：
暴露度报告输出“主要风险因子”与“不确定性提示”（含授权链/人工尽调建议和
待核实事件提醒），并在 UI 案例页结构化展示。

---

## 案例 D：媒体报道置信度不足 / 相互矛盾

**预期**：系统不把低置信度或传闻当作确定事实，条目保持“待核实”并展示来源，
提示人工介入；冲突报道不产生确定结论。

**实测**：

| 行为 | 当前状态 |
| --- | --- |
| EVT-03 入库标注 | verify + low confidence + rumor（✅） |
| 暴露度计算 | 只统计 active 事件，EVT-03 不计入事件强度（✅ 保守） |
| 事件库展示 | 进入“待核实”列表（✅） |
| 冲突报道建模 | 无“同一议题多来源相互矛盾”的数据结构或比较逻辑（缺少） |
| 情景推演入口 | UI 允许勾选“（待核实）”事件直接推演，且无显著风险提示（需处理） |

**结论（已实现）**：保守入库与不参与评分的行为正确；里程碑 4 进一步实现：
`shocks_from_events` 默认忽略 verify 事件；UI 勾选待核实事件时自动忽略并提示；
仅 `include_pending=True` 时作为“假设分析”显式启用。冲突来源比较留作已知局限。

---

## 汇总：里程碑 4 需要决策/处理的事项

| 编号 | 事项 | 类型 |
| --- | --- | --- |
| 1 | ~~案例 A/B 口径~~：采用推荐口径，不改种子数据 | 已决策 |
| 2 | 暴露度报告与 UI 增加“上游不明 → 高不确定性 + 行动建议”输出 | ✅ 已实现 |
| 3 | 待核实事件默认不参与推演，勾选时显式警告 | ✅ 已实现 |
| 4 | 补充“同一议题冲突报道”的数据结构与案例 D 演示 | 已知局限（可后续迭代） |
| 5 | 自动化案例回归脚本与 UI 案例页 | ✅ 已实现 |

## 里程碑 4 执行记录（2026-09-07）

代码与文档改动：

- `chainshield/risk.py`：暴露度报告新增“主要风险因子”“不确定性提示”；
- `chainshield/scenario.py`：断供后新增订单风险提示；`shocks_from_events`
  默认忽略 verify 事件，新增 `include_pending` 假设分析模式；
- `app.py`：多事件推演忽略待核实事件并提示；新增“6 案例与边界演示”页；
- `scripts/cases.py`：案例 A–D 自动化回归脚本；
- 本文件与 `README.md`、`docs/iteration_log.md` 同步更新。

验证结果：

```text
python scripts/cases.py → 案例 A/B/C/D 全部通过 ✅
python scripts/demo.py  → 冒烟演示通过，暴露度排序与推演输出保持基线
```
